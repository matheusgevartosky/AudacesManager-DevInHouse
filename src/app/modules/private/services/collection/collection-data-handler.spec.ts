import { CollectionDataHandler } from './collection-data-handler';

describe('CollectionDataHandler', () => {
  it('should create an instance', () => {
    expect(new CollectionDataHandler()).toBeTruthy();
  });
});
